/**
 * Function to check if the value checked its boolean
 * @param  {Number} number value that wants to be check
 * @return {boolean} True if its a number otherwise its false
 */
var isNumber = function(number){
  return !isNaN(number);
}

export default isNumber;
